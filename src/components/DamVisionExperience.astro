---
import DamVisionCard from './DamVisionCard.astro';
import DamVisionPage from './DamVisionPage.astro';
import type { AppLang } from '../i18n/utils';

interface Props {
  lang: AppLang;
  title: string;
  description: string;
  version: string;
  initialOpen?: boolean;
  initialHidden?: boolean;
}

const { lang, title, description, version, initialOpen = false, initialHidden = false } = Astro.props;
---
<section
  class={`dam-experience ${initialOpen ? 'is-focused-project' : ''} ${initialHidden ? 'is-hidden-project' : ''}`}
  data-project-panel
  data-project-route="dam-vision"
>
  <div class={`dam-shell ${initialOpen ? 'is-open' : ''}`} data-project-shell>
    <div class="dam-compact" data-project-compact>
      <DamVisionCard
        lang={lang}
        title={title}
        description={description}
        version={version}
      />
    </div>

    <div class="dam-expanded">
      <div class="dam-expanded-inner">
        <DamVisionPage lang={lang} version={version} />
      </div>
    </div>
  </div>
</section>

<style>
  .dam-experience {
    animation: fadeIn 0.6s ease forwards;
  }

  .dam-shell {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0;
    overflow: visible;
    z-index: 0;
  }

  .dam-shell.is-open,
  .dam-shell.is-closing {
    z-index: 2;
  }

  .dam-compact {
    position: relative;
    z-index: 2;
    transform-origin: top center;
    transition:
      opacity 0.34s ease,
      transform 0.64s cubic-bezier(0.22, 1, 0.36, 1),
      filter 0.46s ease;
    will-change: opacity, transform, filter;
  }

  .dam-expanded {
    display: grid;
    grid-template-rows: 0fr;
    opacity: 0;
    transform: translateY(18px) scale(0.99);
    transform-origin: top center;
    transition:
      grid-template-rows 0.66s cubic-bezier(0.22, 1, 0.36, 1),
      opacity 0.48s ease,
      transform 0.66s cubic-bezier(0.22, 1, 0.36, 1);
    will-change: opacity, transform, grid-template-rows;
  }

  .dam-expanded-inner {
    min-height: 0;
    overflow: hidden;
  }

  .dam-shell.is-open,
  .dam-shell.is-closing {
    min-height: var(--project-compact-height, 220px);
  }

  .dam-shell.is-open .dam-compact {
    position: absolute;
    inset: 0 0 auto 0;
    transform: translateY(-14px) scale(0.982);
    pointer-events: none;
    filter: blur(6px);
  }

  .dam-shell.is-open .dam-compact {
    opacity: 0;
  }

  .dam-shell.is-closing .dam-compact {
    position: absolute;
    inset: 0 0 auto 0;
    opacity: 1;
    transform: translateY(0) scale(1);
    pointer-events: none;
    filter: blur(0);
    transition-delay: 0.1s, 0.1s, 0.1s;
  }

  .dam-shell.is-open .dam-expanded,
  .dam-shell.is-closing .dam-expanded {
    grid-template-rows: 1fr;
    opacity: 1;
    transform: translateY(0);
  }

  .dam-shell.is-closing .dam-expanded {
    grid-template-rows: 0fr;
    opacity: 0;
    transform: translateY(14px) scale(0.992);
  }

  @media (prefers-reduced-motion: reduce) {
    .dam-compact,
    .dam-expanded {
      transition: none;
    }
  }
</style>
